# Keep the build context small — the image only needs requirements.txt,
# src/ and webui/ (see Dockerfile COPY lines).
.git/
data/
.venv/
__pycache__/
**/__pycache__/
.pytest_cache/
tests/
kometa/
*.md
