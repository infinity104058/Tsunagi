# Pinned runtime dependencies (P1-2) — the Docker image installs exactly
# these, so an unchanged commit builds the same image weeks apart.
# Pins captured 2026-08-05 from the versions passing the offline test suite.
# Loose ranges live in requirements.in; test-only deps in requirements-dev.txt.
plexapi==4.18.2
httpx==0.28.1
aiosqlite==0.22.1
lxml==6.1.1
PyYAML==6.0.3
fastapi==0.141.1
uvicorn[standard]==0.52.1
